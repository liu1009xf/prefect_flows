name: Register Flow using Github Storage 
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    container: prefecthq/prefect:2-python3.10-conda
    env: 
      KEY: ${{secrets.PREFECT_API_KEY}}
    steps:
    # - uses: actions/checkout@v2
    # - uses: BSFishy/pip-action@v1
      # with:
        # requirements: requirements.txt
    # - name: Show options
    #   run: prefect -h
    # - name: set backEnd to client
    #   run: prefect backend cloud
    - name: Authenticate to Prefect dependencies
      run: prefect cloud login -k $KEY
    - name: Register flow
      run: deploy
